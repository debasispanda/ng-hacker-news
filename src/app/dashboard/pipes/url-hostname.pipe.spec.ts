import { UrlHostnamePipe } from './url-hostname.pipe';

describe('UrlHostnamePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlHostnamePipe();
    expect(pipe).toBeTruthy();
  });
});
